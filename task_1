"""
Задание 1.

Докажите, что словари обрабатываются быстрее, чем списки.

Реализуйте две функции, в первой нужно заполнить элементами список, во второй-словарь
Сделайте замеры времени выполнения каждой из функций

Подсказка: для замеров воспользуйтесь модулем time (см. примеры урока 1)

Примечание: eсли вы уже знаете, что такое декоратор и как его реализовать,
то реализуйте ф-цию-декоратор и пусть она считает время
И примените ее к двум своим функциям.
"""

import time


def new_list(n):
    start_val = time.time()
    my_list = []
    for i in range(n):
        my_list.append(i)
    end_val = time.time()
    return my_list, end_val - start_val


def new_dict(n):
    start_val = time.time()
    my_dict = {}
    for i in range(n):
        my_dict[i] = i
    end_val = time.time()
    return my_dict, end_val - start_val


for i in range(1):
    print(f'Операция заполнения элементами списка заняло: {new_list(10000000)[1]} сек')
for i in range(1):
    print(f'Операция заполнения элементами словаря заняло: {new_dict(10000000)[1]} сек')
