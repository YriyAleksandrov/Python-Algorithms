"""
7.	Напишите программу, доказывающую или проверяющую, что для множества
натуральных чисел выполняется равенство: 1+2+...+n = n(n+1)/2,
 где n - любое натуральное число.

 Решите через рекурсию. Решение через цикл не принимается.
Для оценки Отлично в этом блоке необходимо выполнить 5 заданий из 7
"""


def func(num, row=1, left=1):
    if row == num:
        right = num * (num + 1) / 2
        if left == right:
            print('Проверка завершилась равенством левой и правой частей')
        else:
            print('Проверка завершилась неравенством левой и правой частей')
    else:
        row += 1
        left += row
        return func(num, row, left)


try:
    user_num = int(input('Введите натуральное число: '))
    func(user_num)
except ValueError:
    print('Ошибка. Введено некорректное значение.')
